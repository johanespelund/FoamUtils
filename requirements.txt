# Development dependencies — not used by the build system.
# Install the package with: pip install .
# To pin exact versions for reproducible environments, run:
#   pip install . && pip freeze > requirements-lock.txt
click>=8.0
matplotlib>=3.5
numpy>=1.22
pandas>=1.4
PyFoam>=2023.0
scienceplots>=2.0
